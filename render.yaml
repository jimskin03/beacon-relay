services:
  - type: web
    name: beacon-relay
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: APP_ORIGIN
        sync: false
